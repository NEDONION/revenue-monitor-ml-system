[project]
name = "revenue-monitor-ml-system"
version = "0.1.0"
description = "Revenue anomaly detection system"
requires-python = ">=3.10"
readme = "README.md"

[tool.uv]
dev-dependencies = []

[tool.uv]
dev-dependencies = []
